{
	"database": {
		"host": "localhost",
		"database": "ukhasnet",
		"user": "ukhasnet",
		"password": "secret"
	},

	"lock_file": "$ENV{'HOME'} . '/run/ukhasnet.pid'",

	"syslog": {
		"facility": "local1"
	}
}
